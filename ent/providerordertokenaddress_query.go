// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/paycrest/paycrest-protocol/ent/predicate"
	"github.com/paycrest/paycrest-protocol/ent/providerordertoken"
	"github.com/paycrest/paycrest-protocol/ent/providerordertokenaddress"
)

// ProviderOrderTokenAddressQuery is the builder for querying ProviderOrderTokenAddress entities.
type ProviderOrderTokenAddressQuery struct {
	config
	ctx                    *QueryContext
	order                  []providerordertokenaddress.OrderOption
	inters                 []Interceptor
	predicates             []predicate.ProviderOrderTokenAddress
	withProviderordertoken *ProviderOrderTokenQuery
	withFKs                bool
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ProviderOrderTokenAddressQuery builder.
func (potaq *ProviderOrderTokenAddressQuery) Where(ps ...predicate.ProviderOrderTokenAddress) *ProviderOrderTokenAddressQuery {
	potaq.predicates = append(potaq.predicates, ps...)
	return potaq
}

// Limit the number of records to be returned by this query.
func (potaq *ProviderOrderTokenAddressQuery) Limit(limit int) *ProviderOrderTokenAddressQuery {
	potaq.ctx.Limit = &limit
	return potaq
}

// Offset to start from.
func (potaq *ProviderOrderTokenAddressQuery) Offset(offset int) *ProviderOrderTokenAddressQuery {
	potaq.ctx.Offset = &offset
	return potaq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (potaq *ProviderOrderTokenAddressQuery) Unique(unique bool) *ProviderOrderTokenAddressQuery {
	potaq.ctx.Unique = &unique
	return potaq
}

// Order specifies how the records should be ordered.
func (potaq *ProviderOrderTokenAddressQuery) Order(o ...providerordertokenaddress.OrderOption) *ProviderOrderTokenAddressQuery {
	potaq.order = append(potaq.order, o...)
	return potaq
}

// QueryProviderordertoken chains the current query on the "providerordertoken" edge.
func (potaq *ProviderOrderTokenAddressQuery) QueryProviderordertoken() *ProviderOrderTokenQuery {
	query := (&ProviderOrderTokenClient{config: potaq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := potaq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := potaq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(providerordertokenaddress.Table, providerordertokenaddress.FieldID, selector),
			sqlgraph.To(providerordertoken.Table, providerordertoken.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, providerordertokenaddress.ProviderordertokenTable, providerordertokenaddress.ProviderordertokenColumn),
		)
		fromU = sqlgraph.SetNeighbors(potaq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first ProviderOrderTokenAddress entity from the query.
// Returns a *NotFoundError when no ProviderOrderTokenAddress was found.
func (potaq *ProviderOrderTokenAddressQuery) First(ctx context.Context) (*ProviderOrderTokenAddress, error) {
	nodes, err := potaq.Limit(1).All(setContextOp(ctx, potaq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{providerordertokenaddress.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (potaq *ProviderOrderTokenAddressQuery) FirstX(ctx context.Context) *ProviderOrderTokenAddress {
	node, err := potaq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first ProviderOrderTokenAddress ID from the query.
// Returns a *NotFoundError when no ProviderOrderTokenAddress ID was found.
func (potaq *ProviderOrderTokenAddressQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = potaq.Limit(1).IDs(setContextOp(ctx, potaq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{providerordertokenaddress.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (potaq *ProviderOrderTokenAddressQuery) FirstIDX(ctx context.Context) int {
	id, err := potaq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single ProviderOrderTokenAddress entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one ProviderOrderTokenAddress entity is found.
// Returns a *NotFoundError when no ProviderOrderTokenAddress entities are found.
func (potaq *ProviderOrderTokenAddressQuery) Only(ctx context.Context) (*ProviderOrderTokenAddress, error) {
	nodes, err := potaq.Limit(2).All(setContextOp(ctx, potaq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{providerordertokenaddress.Label}
	default:
		return nil, &NotSingularError{providerordertokenaddress.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (potaq *ProviderOrderTokenAddressQuery) OnlyX(ctx context.Context) *ProviderOrderTokenAddress {
	node, err := potaq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only ProviderOrderTokenAddress ID in the query.
// Returns a *NotSingularError when more than one ProviderOrderTokenAddress ID is found.
// Returns a *NotFoundError when no entities are found.
func (potaq *ProviderOrderTokenAddressQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = potaq.Limit(2).IDs(setContextOp(ctx, potaq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{providerordertokenaddress.Label}
	default:
		err = &NotSingularError{providerordertokenaddress.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (potaq *ProviderOrderTokenAddressQuery) OnlyIDX(ctx context.Context) int {
	id, err := potaq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of ProviderOrderTokenAddresses.
func (potaq *ProviderOrderTokenAddressQuery) All(ctx context.Context) ([]*ProviderOrderTokenAddress, error) {
	ctx = setContextOp(ctx, potaq.ctx, "All")
	if err := potaq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*ProviderOrderTokenAddress, *ProviderOrderTokenAddressQuery]()
	return withInterceptors[[]*ProviderOrderTokenAddress](ctx, potaq, qr, potaq.inters)
}

// AllX is like All, but panics if an error occurs.
func (potaq *ProviderOrderTokenAddressQuery) AllX(ctx context.Context) []*ProviderOrderTokenAddress {
	nodes, err := potaq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of ProviderOrderTokenAddress IDs.
func (potaq *ProviderOrderTokenAddressQuery) IDs(ctx context.Context) (ids []int, err error) {
	if potaq.ctx.Unique == nil && potaq.path != nil {
		potaq.Unique(true)
	}
	ctx = setContextOp(ctx, potaq.ctx, "IDs")
	if err = potaq.Select(providerordertokenaddress.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (potaq *ProviderOrderTokenAddressQuery) IDsX(ctx context.Context) []int {
	ids, err := potaq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (potaq *ProviderOrderTokenAddressQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, potaq.ctx, "Count")
	if err := potaq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, potaq, querierCount[*ProviderOrderTokenAddressQuery](), potaq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (potaq *ProviderOrderTokenAddressQuery) CountX(ctx context.Context) int {
	count, err := potaq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (potaq *ProviderOrderTokenAddressQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, potaq.ctx, "Exist")
	switch _, err := potaq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (potaq *ProviderOrderTokenAddressQuery) ExistX(ctx context.Context) bool {
	exist, err := potaq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ProviderOrderTokenAddressQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (potaq *ProviderOrderTokenAddressQuery) Clone() *ProviderOrderTokenAddressQuery {
	if potaq == nil {
		return nil
	}
	return &ProviderOrderTokenAddressQuery{
		config:                 potaq.config,
		ctx:                    potaq.ctx.Clone(),
		order:                  append([]providerordertokenaddress.OrderOption{}, potaq.order...),
		inters:                 append([]Interceptor{}, potaq.inters...),
		predicates:             append([]predicate.ProviderOrderTokenAddress{}, potaq.predicates...),
		withProviderordertoken: potaq.withProviderordertoken.Clone(),
		// clone intermediate query.
		sql:  potaq.sql.Clone(),
		path: potaq.path,
	}
}

// WithProviderordertoken tells the query-builder to eager-load the nodes that are connected to
// the "providerordertoken" edge. The optional arguments are used to configure the query builder of the edge.
func (potaq *ProviderOrderTokenAddressQuery) WithProviderordertoken(opts ...func(*ProviderOrderTokenQuery)) *ProviderOrderTokenAddressQuery {
	query := (&ProviderOrderTokenClient{config: potaq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	potaq.withProviderordertoken = query
	return potaq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Network providerordertokenaddress.Network `json:"network,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.ProviderOrderTokenAddress.Query().
//		GroupBy(providerordertokenaddress.FieldNetwork).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (potaq *ProviderOrderTokenAddressQuery) GroupBy(field string, fields ...string) *ProviderOrderTokenAddressGroupBy {
	potaq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &ProviderOrderTokenAddressGroupBy{build: potaq}
	grbuild.flds = &potaq.ctx.Fields
	grbuild.label = providerordertokenaddress.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Network providerordertokenaddress.Network `json:"network,omitempty"`
//	}
//
//	client.ProviderOrderTokenAddress.Query().
//		Select(providerordertokenaddress.FieldNetwork).
//		Scan(ctx, &v)
func (potaq *ProviderOrderTokenAddressQuery) Select(fields ...string) *ProviderOrderTokenAddressSelect {
	potaq.ctx.Fields = append(potaq.ctx.Fields, fields...)
	sbuild := &ProviderOrderTokenAddressSelect{ProviderOrderTokenAddressQuery: potaq}
	sbuild.label = providerordertokenaddress.Label
	sbuild.flds, sbuild.scan = &potaq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a ProviderOrderTokenAddressSelect configured with the given aggregations.
func (potaq *ProviderOrderTokenAddressQuery) Aggregate(fns ...AggregateFunc) *ProviderOrderTokenAddressSelect {
	return potaq.Select().Aggregate(fns...)
}

func (potaq *ProviderOrderTokenAddressQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range potaq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, potaq); err != nil {
				return err
			}
		}
	}
	for _, f := range potaq.ctx.Fields {
		if !providerordertokenaddress.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if potaq.path != nil {
		prev, err := potaq.path(ctx)
		if err != nil {
			return err
		}
		potaq.sql = prev
	}
	return nil
}

func (potaq *ProviderOrderTokenAddressQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*ProviderOrderTokenAddress, error) {
	var (
		nodes       = []*ProviderOrderTokenAddress{}
		withFKs     = potaq.withFKs
		_spec       = potaq.querySpec()
		loadedTypes = [1]bool{
			potaq.withProviderordertoken != nil,
		}
	)
	if potaq.withProviderordertoken != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, providerordertokenaddress.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*ProviderOrderTokenAddress).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &ProviderOrderTokenAddress{config: potaq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, potaq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := potaq.withProviderordertoken; query != nil {
		if err := potaq.loadProviderordertoken(ctx, query, nodes, nil,
			func(n *ProviderOrderTokenAddress, e *ProviderOrderToken) { n.Edges.Providerordertoken = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (potaq *ProviderOrderTokenAddressQuery) loadProviderordertoken(ctx context.Context, query *ProviderOrderTokenQuery, nodes []*ProviderOrderTokenAddress, init func(*ProviderOrderTokenAddress), assign func(*ProviderOrderTokenAddress, *ProviderOrderToken)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*ProviderOrderTokenAddress)
	for i := range nodes {
		if nodes[i].provider_order_token_addresses == nil {
			continue
		}
		fk := *nodes[i].provider_order_token_addresses
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(providerordertoken.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "provider_order_token_addresses" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (potaq *ProviderOrderTokenAddressQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := potaq.querySpec()
	_spec.Node.Columns = potaq.ctx.Fields
	if len(potaq.ctx.Fields) > 0 {
		_spec.Unique = potaq.ctx.Unique != nil && *potaq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, potaq.driver, _spec)
}

func (potaq *ProviderOrderTokenAddressQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(providerordertokenaddress.Table, providerordertokenaddress.Columns, sqlgraph.NewFieldSpec(providerordertokenaddress.FieldID, field.TypeInt))
	_spec.From = potaq.sql
	if unique := potaq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if potaq.path != nil {
		_spec.Unique = true
	}
	if fields := potaq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, providerordertokenaddress.FieldID)
		for i := range fields {
			if fields[i] != providerordertokenaddress.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := potaq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := potaq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := potaq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := potaq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (potaq *ProviderOrderTokenAddressQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(potaq.driver.Dialect())
	t1 := builder.Table(providerordertokenaddress.Table)
	columns := potaq.ctx.Fields
	if len(columns) == 0 {
		columns = providerordertokenaddress.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if potaq.sql != nil {
		selector = potaq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if potaq.ctx.Unique != nil && *potaq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range potaq.predicates {
		p(selector)
	}
	for _, p := range potaq.order {
		p(selector)
	}
	if offset := potaq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := potaq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ProviderOrderTokenAddressGroupBy is the group-by builder for ProviderOrderTokenAddress entities.
type ProviderOrderTokenAddressGroupBy struct {
	selector
	build *ProviderOrderTokenAddressQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (potagb *ProviderOrderTokenAddressGroupBy) Aggregate(fns ...AggregateFunc) *ProviderOrderTokenAddressGroupBy {
	potagb.fns = append(potagb.fns, fns...)
	return potagb
}

// Scan applies the selector query and scans the result into the given value.
func (potagb *ProviderOrderTokenAddressGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, potagb.build.ctx, "GroupBy")
	if err := potagb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ProviderOrderTokenAddressQuery, *ProviderOrderTokenAddressGroupBy](ctx, potagb.build, potagb, potagb.build.inters, v)
}

func (potagb *ProviderOrderTokenAddressGroupBy) sqlScan(ctx context.Context, root *ProviderOrderTokenAddressQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(potagb.fns))
	for _, fn := range potagb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*potagb.flds)+len(potagb.fns))
		for _, f := range *potagb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*potagb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := potagb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// ProviderOrderTokenAddressSelect is the builder for selecting fields of ProviderOrderTokenAddress entities.
type ProviderOrderTokenAddressSelect struct {
	*ProviderOrderTokenAddressQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (potas *ProviderOrderTokenAddressSelect) Aggregate(fns ...AggregateFunc) *ProviderOrderTokenAddressSelect {
	potas.fns = append(potas.fns, fns...)
	return potas
}

// Scan applies the selector query and scans the result into the given value.
func (potas *ProviderOrderTokenAddressSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, potas.ctx, "Select")
	if err := potas.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ProviderOrderTokenAddressQuery, *ProviderOrderTokenAddressSelect](ctx, potas.ProviderOrderTokenAddressQuery, potas, potas.inters, v)
}

func (potas *ProviderOrderTokenAddressSelect) sqlScan(ctx context.Context, root *ProviderOrderTokenAddressQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(potas.fns))
	for _, fn := range potas.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*potas.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := potas.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
