// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"
	"github.com/paycrest/protocol/ent/fiatcurrency"
	"github.com/paycrest/protocol/ent/financialinstitution"
	"github.com/paycrest/protocol/ent/predicate"
)

// FinancialInstitutionQuery is the builder for querying FinancialInstitution entities.
type FinancialInstitutionQuery struct {
	config
	ctx        *QueryContext
	order      []financialinstitution.OrderOption
	inters     []Interceptor
	predicates []predicate.FinancialInstitution
	withFiat   *FiatCurrencyQuery
	withFKs    bool
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the FinancialInstitutionQuery builder.
func (fiq *FinancialInstitutionQuery) Where(ps ...predicate.FinancialInstitution) *FinancialInstitutionQuery {
	fiq.predicates = append(fiq.predicates, ps...)
	return fiq
}

// Limit the number of records to be returned by this query.
func (fiq *FinancialInstitutionQuery) Limit(limit int) *FinancialInstitutionQuery {
	fiq.ctx.Limit = &limit
	return fiq
}

// Offset to start from.
func (fiq *FinancialInstitutionQuery) Offset(offset int) *FinancialInstitutionQuery {
	fiq.ctx.Offset = &offset
	return fiq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (fiq *FinancialInstitutionQuery) Unique(unique bool) *FinancialInstitutionQuery {
	fiq.ctx.Unique = &unique
	return fiq
}

// Order specifies how the records should be ordered.
func (fiq *FinancialInstitutionQuery) Order(o ...financialinstitution.OrderOption) *FinancialInstitutionQuery {
	fiq.order = append(fiq.order, o...)
	return fiq
}

// QueryFiat chains the current query on the "fiat" edge.
func (fiq *FinancialInstitutionQuery) QueryFiat() *FiatCurrencyQuery {
	query := (&FiatCurrencyClient{config: fiq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := fiq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := fiq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(financialinstitution.Table, financialinstitution.FieldID, selector),
			sqlgraph.To(fiatcurrency.Table, fiatcurrency.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, financialinstitution.FiatTable, financialinstitution.FiatColumn),
		)
		fromU = sqlgraph.SetNeighbors(fiq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first FinancialInstitution entity from the query.
// Returns a *NotFoundError when no FinancialInstitution was found.
func (fiq *FinancialInstitutionQuery) First(ctx context.Context) (*FinancialInstitution, error) {
	nodes, err := fiq.Limit(1).All(setContextOp(ctx, fiq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{financialinstitution.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (fiq *FinancialInstitutionQuery) FirstX(ctx context.Context) *FinancialInstitution {
	node, err := fiq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first FinancialInstitution ID from the query.
// Returns a *NotFoundError when no FinancialInstitution ID was found.
func (fiq *FinancialInstitutionQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = fiq.Limit(1).IDs(setContextOp(ctx, fiq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{financialinstitution.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (fiq *FinancialInstitutionQuery) FirstIDX(ctx context.Context) int {
	id, err := fiq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single FinancialInstitution entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one FinancialInstitution entity is found.
// Returns a *NotFoundError when no FinancialInstitution entities are found.
func (fiq *FinancialInstitutionQuery) Only(ctx context.Context) (*FinancialInstitution, error) {
	nodes, err := fiq.Limit(2).All(setContextOp(ctx, fiq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{financialinstitution.Label}
	default:
		return nil, &NotSingularError{financialinstitution.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (fiq *FinancialInstitutionQuery) OnlyX(ctx context.Context) *FinancialInstitution {
	node, err := fiq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only FinancialInstitution ID in the query.
// Returns a *NotSingularError when more than one FinancialInstitution ID is found.
// Returns a *NotFoundError when no entities are found.
func (fiq *FinancialInstitutionQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = fiq.Limit(2).IDs(setContextOp(ctx, fiq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{financialinstitution.Label}
	default:
		err = &NotSingularError{financialinstitution.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (fiq *FinancialInstitutionQuery) OnlyIDX(ctx context.Context) int {
	id, err := fiq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of FinancialInstitutions.
func (fiq *FinancialInstitutionQuery) All(ctx context.Context) ([]*FinancialInstitution, error) {
	ctx = setContextOp(ctx, fiq.ctx, "All")
	if err := fiq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*FinancialInstitution, *FinancialInstitutionQuery]()
	return withInterceptors[[]*FinancialInstitution](ctx, fiq, qr, fiq.inters)
}

// AllX is like All, but panics if an error occurs.
func (fiq *FinancialInstitutionQuery) AllX(ctx context.Context) []*FinancialInstitution {
	nodes, err := fiq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of FinancialInstitution IDs.
func (fiq *FinancialInstitutionQuery) IDs(ctx context.Context) (ids []int, err error) {
	if fiq.ctx.Unique == nil && fiq.path != nil {
		fiq.Unique(true)
	}
	ctx = setContextOp(ctx, fiq.ctx, "IDs")
	if err = fiq.Select(financialinstitution.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (fiq *FinancialInstitutionQuery) IDsX(ctx context.Context) []int {
	ids, err := fiq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (fiq *FinancialInstitutionQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, fiq.ctx, "Count")
	if err := fiq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, fiq, querierCount[*FinancialInstitutionQuery](), fiq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (fiq *FinancialInstitutionQuery) CountX(ctx context.Context) int {
	count, err := fiq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (fiq *FinancialInstitutionQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, fiq.ctx, "Exist")
	switch _, err := fiq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (fiq *FinancialInstitutionQuery) ExistX(ctx context.Context) bool {
	exist, err := fiq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the FinancialInstitutionQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (fiq *FinancialInstitutionQuery) Clone() *FinancialInstitutionQuery {
	if fiq == nil {
		return nil
	}
	return &FinancialInstitutionQuery{
		config:     fiq.config,
		ctx:        fiq.ctx.Clone(),
		order:      append([]financialinstitution.OrderOption{}, fiq.order...),
		inters:     append([]Interceptor{}, fiq.inters...),
		predicates: append([]predicate.FinancialInstitution{}, fiq.predicates...),
		withFiat:   fiq.withFiat.Clone(),
		// clone intermediate query.
		sql:  fiq.sql.Clone(),
		path: fiq.path,
	}
}

// WithFiat tells the query-builder to eager-load the nodes that are connected to
// the "fiat" edge. The optional arguments are used to configure the query builder of the edge.
func (fiq *FinancialInstitutionQuery) WithFiat(opts ...func(*FiatCurrencyQuery)) *FinancialInstitutionQuery {
	query := (&FiatCurrencyClient{config: fiq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	fiq.withFiat = query
	return fiq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Code string `json:"code,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.FinancialInstitution.Query().
//		GroupBy(financialinstitution.FieldCode).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (fiq *FinancialInstitutionQuery) GroupBy(field string, fields ...string) *FinancialInstitutionGroupBy {
	fiq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &FinancialInstitutionGroupBy{build: fiq}
	grbuild.flds = &fiq.ctx.Fields
	grbuild.label = financialinstitution.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Code string `json:"code,omitempty"`
//	}
//
//	client.FinancialInstitution.Query().
//		Select(financialinstitution.FieldCode).
//		Scan(ctx, &v)
func (fiq *FinancialInstitutionQuery) Select(fields ...string) *FinancialInstitutionSelect {
	fiq.ctx.Fields = append(fiq.ctx.Fields, fields...)
	sbuild := &FinancialInstitutionSelect{FinancialInstitutionQuery: fiq}
	sbuild.label = financialinstitution.Label
	sbuild.flds, sbuild.scan = &fiq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a FinancialInstitutionSelect configured with the given aggregations.
func (fiq *FinancialInstitutionQuery) Aggregate(fns ...AggregateFunc) *FinancialInstitutionSelect {
	return fiq.Select().Aggregate(fns...)
}

func (fiq *FinancialInstitutionQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range fiq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, fiq); err != nil {
				return err
			}
		}
	}
	for _, f := range fiq.ctx.Fields {
		if !financialinstitution.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if fiq.path != nil {
		prev, err := fiq.path(ctx)
		if err != nil {
			return err
		}
		fiq.sql = prev
	}
	return nil
}

func (fiq *FinancialInstitutionQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*FinancialInstitution, error) {
	var (
		nodes       = []*FinancialInstitution{}
		withFKs     = fiq.withFKs
		_spec       = fiq.querySpec()
		loadedTypes = [1]bool{
			fiq.withFiat != nil,
		}
	)
	if fiq.withFiat != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, financialinstitution.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*FinancialInstitution).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &FinancialInstitution{config: fiq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, fiq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := fiq.withFiat; query != nil {
		if err := fiq.loadFiat(ctx, query, nodes, nil,
			func(n *FinancialInstitution, e *FiatCurrency) { n.Edges.Fiat = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (fiq *FinancialInstitutionQuery) loadFiat(ctx context.Context, query *FiatCurrencyQuery, nodes []*FinancialInstitution, init func(*FinancialInstitution), assign func(*FinancialInstitution, *FiatCurrency)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*FinancialInstitution)
	for i := range nodes {
		if nodes[i].fiat_currency_financial_institutions == nil {
			continue
		}
		fk := *nodes[i].fiat_currency_financial_institutions
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(fiatcurrency.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "fiat_currency_financial_institutions" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (fiq *FinancialInstitutionQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := fiq.querySpec()
	_spec.Node.Columns = fiq.ctx.Fields
	if len(fiq.ctx.Fields) > 0 {
		_spec.Unique = fiq.ctx.Unique != nil && *fiq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, fiq.driver, _spec)
}

func (fiq *FinancialInstitutionQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(financialinstitution.Table, financialinstitution.Columns, sqlgraph.NewFieldSpec(financialinstitution.FieldID, field.TypeInt))
	_spec.From = fiq.sql
	if unique := fiq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if fiq.path != nil {
		_spec.Unique = true
	}
	if fields := fiq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, financialinstitution.FieldID)
		for i := range fields {
			if fields[i] != financialinstitution.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := fiq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := fiq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := fiq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := fiq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (fiq *FinancialInstitutionQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(fiq.driver.Dialect())
	t1 := builder.Table(financialinstitution.Table)
	columns := fiq.ctx.Fields
	if len(columns) == 0 {
		columns = financialinstitution.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if fiq.sql != nil {
		selector = fiq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if fiq.ctx.Unique != nil && *fiq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range fiq.predicates {
		p(selector)
	}
	for _, p := range fiq.order {
		p(selector)
	}
	if offset := fiq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := fiq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// FinancialInstitutionGroupBy is the group-by builder for FinancialInstitution entities.
type FinancialInstitutionGroupBy struct {
	selector
	build *FinancialInstitutionQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (figb *FinancialInstitutionGroupBy) Aggregate(fns ...AggregateFunc) *FinancialInstitutionGroupBy {
	figb.fns = append(figb.fns, fns...)
	return figb
}

// Scan applies the selector query and scans the result into the given value.
func (figb *FinancialInstitutionGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, figb.build.ctx, "GroupBy")
	if err := figb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*FinancialInstitutionQuery, *FinancialInstitutionGroupBy](ctx, figb.build, figb, figb.build.inters, v)
}

func (figb *FinancialInstitutionGroupBy) sqlScan(ctx context.Context, root *FinancialInstitutionQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(figb.fns))
	for _, fn := range figb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*figb.flds)+len(figb.fns))
		for _, f := range *figb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*figb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := figb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// FinancialInstitutionSelect is the builder for selecting fields of FinancialInstitution entities.
type FinancialInstitutionSelect struct {
	*FinancialInstitutionQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (fis *FinancialInstitutionSelect) Aggregate(fns ...AggregateFunc) *FinancialInstitutionSelect {
	fis.fns = append(fis.fns, fns...)
	return fis
}

// Scan applies the selector query and scans the result into the given value.
func (fis *FinancialInstitutionSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, fis.ctx, "Select")
	if err := fis.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*FinancialInstitutionQuery, *FinancialInstitutionSelect](ctx, fis.FinancialInstitutionQuery, fis, fis.inters, v)
}

func (fis *FinancialInstitutionSelect) sqlScan(ctx context.Context, root *FinancialInstitutionQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(fis.fns))
	for _, fn := range fis.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*fis.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := fis.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
